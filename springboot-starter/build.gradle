dependencies {
    // Project Modules
    api project(':' + rootProject.name + '-core')

    // SpringBoot
    api group: 'org.springframework.boot', name: 'spring-boot-starter', version: '2.4.4'
}

// Artifact Info
project.ext {
    artifactName = 'GCF4J-SpringBoot-Starter'
    artifactDescription = 'GCF4J - SpringBoot Starter'
}

// Deployment Script
apply from: '../deployment.gradle'
